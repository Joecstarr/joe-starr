---
import "~/styles/cv.css";
import { type CollectionEntry } from "astro:content";
import CVSection from "~/components/cv/Section.astro";
import CVContact from "~/components/cv/Contact.astro";
import CVSkill from "~/components/cv/Skill.astro";
import { displayPartsToString } from "typescript";
import { Icon } from "astro-icon/components";
interface Props {
    post: CollectionEntry<"cv">;
    theme: Object;
    data: Object;
    display_skills: boolean;
}
const { post, theme, data, display_skills } = Astro.props;
---

<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{data.header.name} - Resume</title>
        <!-- fontawesome -->
        <link
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css"
        />
        <script defer src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/js/all.min.js"
        ></script>

        <style
            define:vars={{
                textColor: theme.textColor,
                printTextColor: theme.printTextColor,
                secondaryTextColor: theme.secondaryTextColor,
                backgroundColor: theme.backgroundColor,
                secondaryBackgroundColor: theme.secondaryBackgroundColor,
                primaryColor: theme.primaryColor,
                secondaryColor: theme.secondaryColor,
                contrastTextColor: theme.contrastTextColor,
            }}
            is:global
        >
            @import url("https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100..900;1,100..900&display=swap");

            :root {
                --text-color: var(--textColor);
                --print-text-color: var(--printTextColor);
                --text-secondary-color: var(--secondaryTextColor);
                --background-color: var(--backgroundColor);
                --secondary-background-color: var(--secondaryBackgroundColor);
                --primary-color: var(--primaryColor);
                --secondary-color: var(--secondaryColor);
                --contrast-text-color: var(--contrastTextColor);
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
            }
        </style>
        <link
            rel="stylesheet"
            href="https://cdn.jsdelivr.net/npm/katex@0.16.46/dist/katex.min.css"
            integrity="sha384-hW6ZmmePRD2f/9cuxGE6C9faGprtIBOme5OLUiEjtRKMTN67tY23ur9eAi21H8De"
            crossorigin="anonymous"
        />
        <script
            defer
            src="https://cdn.jsdelivr.net/npm/katex@0.16.46/dist/katex.min.js"
            integrity="sha384-FVvsvR4UzyIP8Y5hVvHjOfjVh+LWV78ll63SYx1t+nuuMPGMAihB8dJ2YsYyg1Wb"
            crossorigin="anonymous"></script>
        <script
            defer
            src="https://cdn.jsdelivr.net/npm/katex@0.16.46/dist/contrib/auto-render.min.js"
            integrity="sha384-bjyGPfbij8/NDKJhSGZNP/khQVgtHUE5exjm4Ydllo42FwIgYsdLO2lXGmRBf5Mz"
            crossorigin="anonymous"></script>
        <script>
            document.addEventListener("DOMContentLoaded", function () {
                renderMathInElement(document.body, {
                    // customised options
                    // • auto-render specific keys, e.g.:
                    delimiters: [
                        { left: "$$", right: "$$", display: true },
                        { left: "$", right: "$", display: false },
                        { left: "\\(", right: "\\)", display: false },
                        { left: "\\[", right: "\\]", display: true },
                    ],
                    // • rendering keys, e.g.:
                    throwOnError: false,
                });
            });
        </script>
    </head>

    <body>
        <div class="head">
            <h1>{data.header.name}</h1>
            <h6 style="text-align:left;margin-bottom:.5em;margin-left:2em;margin-right:2em;">
                {data.header.title}
            </h6>
            <CVContact contacts={data.header.contact} />
            <p>
                {data.header.summary}
            </p>
        </div>

        <div>
            {
                display_skills && (
                    <div>
                        {data.sections.map((item) => (
                            <CVSection section={item} />
                        ))}
                        {data.bullets.map((item) => (
                            <div>
                                <h3 class="section_title" set:html={item.title}>
                                    <Icon class="icon" focusable="false" name={item.icon} />
                                </h3>
                                <div class="hr" />
                                <CVSkill skill={item} />
                            </div>
                        ))}
                    </div>
                )
            }
            {
                !display_skills && (
                    <div style="width:100%;">
                        {data.sections.map((item) => (
                            <CVSection section={item} />
                        ))}
                    </div>
                )
            }
        </div>
    </body>
</html>
